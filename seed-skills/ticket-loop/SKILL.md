---
name: ticket-loop
description: >
  Alias for task-loop. Use when starting a bug, feature, Jira ticket, or any
  prompted task in any repo. Read and follow the task-loop skill — not Jira-only.
---

# Ticket loop

Deprecated name. The loop is **`task-loop`**.

A chat prompt is a task. Jira is optional step 0.

**Read and follow `task-loop` now.** Do not keep a per-repo fork of this workflow.

If the user gave a Jira key/URL **and** `jira-branch-workflow` exists, run that first, then `task-loop` from Intent.
